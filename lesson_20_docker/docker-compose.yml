services:
  api:
    build: .
    command: ["-m", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"

  ui:
    build: .
    command: ["ui.py"]
    environment:
      - API_URL=http://api:8000
    ports:
      - "7860:7860"
    depends_on:
      - api